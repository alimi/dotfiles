# Local/private config goes in the include
[include]
  path = ~/.gitconfig.local
[core]
  excludesfile = ~/.gitignore
[push]
  default = simple
[pull]
  ff = only
[credential]
  helper = osxkeychain
[rebase]
  autoSquash = true
  autoStash = true
